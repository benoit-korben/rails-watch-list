<h1>Lists of movies</h1>
<%= link_to "Add a list", new_list_path, class: "btn btn-primary p-2" %>
<% @lists.each do |list| %>
  <p>
  <%= list.name %>
  <%= link_to "View details", list_path(list) %>
  </p>

<% end %>
